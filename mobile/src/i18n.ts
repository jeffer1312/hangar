// Re-export simples: o mobile usa o m.* do paraglide compilado localmente.
// Este arquivo existe para manter paridade com o core e permitir import único se necessário.
// A compilação é feita via `npm run i18n:compile` que gera ./src/paraglide
export {};
